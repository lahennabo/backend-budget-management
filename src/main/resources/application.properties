spring.application.name=budget-management

# Configuration de la connexion MySQL
spring.datasource.url=${MYSQL_URL}
spring.datasource.username=${MYSQLUSER}
spring.datasource.password=${MYSQLPASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuration de Hibernate / JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

## Configuration de la base de donnees (Utilise les variables Railway, ou Local si absentes)
#spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/budget_db?useSSL=false&serverTimezone=UTC}
#spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
#spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:}
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#
## Configuration de Hibernate / JPA
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true
#
## Forcer le port d ecoute fourni par Railway (Requis pour le deploiement)
#server.port=${PORT:8080}